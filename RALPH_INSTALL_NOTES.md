# Ralph Scaffolding Install Notes

## Installed Files

| File | Status | Notes |
|------|--------|-------|
| `AGENTS.md` | ✅ Created | Tailored with real project commands |
| `IMPLEMENTATION_PLAN.md` | ⚠️ Conflict | Already existed with project gap analysis. Ralph template saved as `IMPLEMENTATION_PLAN.md.ralph`. Existing file preserved. |
| `IMPLEMENTATION_PLAN.md.ralph` | ✅ Created | Ralph's blank template (for reference) |
| `PROMPT_plan.md` | ✅ Created | Adjusted paths: `src/*` → `ambient-server/server/*` + `ambient-server/ios/AmbientApp/*` |
| `PROMPT_build.md` | ✅ Created | Adjusted paths: `src/*` → `ambient-server/` references |
| `loop.sh` | ✅ Created | Made executable (chmod +x) |
| `specs/PRODUCT_SPEC.md` | ✅ Created | Full product specification from original requirements |
| `specs/ARCHITECTURE.md` | ✅ Created | Directory structure, data flow, endpoints, design decisions |

## Conflict Resolution

**`IMPLEMENTATION_PLAN.md`**: The existing file contains a detailed gap analysis comparing code against requirements, with P0/P1/P2/P3 items and resolution tracking. The Ralph template was a blank placeholder (`<!-- Generated by LLM -->`). The existing file was preserved as it contains critical project state. The blank template is at `IMPLEMENTATION_PLAN.md.ralph` for reference.

## AGENTS.md Commands

| Action | Command |
|--------|---------|
| Install deps | `pip install -r ambient-server/requirements.txt` |
| Run tests | `cd ambient-server/server && python3 -m pytest tests/ -v` |
| Start server | `cd ambient-server/server && python3 main.py` |
| E2E test | `cd ambient-server && python3 test_e2e.py` (requires running server) |
| iOS build | `cd ambient-server/ios && xcodegen generate && open AmbientApp.xcodeproj` |

## Follow-ups

- No typecheck or lint tools are configured. Consider adding `mypy` and/or `ruff` if desired.
- `loop.sh` requires the `claude` CLI tool to be installed and authenticated.
- iOS compilation cannot be verified on this VM (no Xcode).
